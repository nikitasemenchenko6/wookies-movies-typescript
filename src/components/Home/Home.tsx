import React from 'react';
import Header from '../Header/Header';
import MoviesList from '../MoviesList/MoviesList';

const Home = () => {
    return (
        <div>
            <MoviesList/>
        </div>
    );
};

export default Home;
